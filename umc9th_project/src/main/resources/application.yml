spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/umc9th_mission?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: Seona!0728

  jpa:
    hibernate:
      ddl-auto: update  # 처음엔 create로 해도 됨
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true

